# Deprecated Files

These files are **legacy code** that was superseded by the dynamic `agent_factory.py` system.

- `reviewer.py` — Replaced by `create_agent_node(reviewer_config)` in `graph.py`
- `worker.py` — Replaced by `create_agent_node(config)` for all worker agents

They are kept here for reference only. **Do not import from this directory.**
